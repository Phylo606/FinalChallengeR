export interface Match {
    $key: string;
    matchTime: string;
    matchDate: string;
    matchLocation: string;
    matchCourt: string;
    courtFeesPaidBy: string;
    amountPaid: string;
    createdBy: string;
}